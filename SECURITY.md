# Security policy

Never commit `.env`, `.env.local`, provider keys, Supabase service-role keys,
database exports or customer data.

Report vulnerabilities privately through GitHub private vulnerability reporting.
If a credential is exposed, revoke or rotate it at the owning provider before
attempting any cleanup.
